# 本番公開チェックリスト（Phase 5）

すべてチェックできたら公開（＝git pushでWEB予約ボタンが全ページに出る）。

## A. 設定の最終確認

- [ ] `settings` の `notification_emails` に店の受信アドレスが入っている
- [ ] `settings` の `reply_to_email` が設定されている
- [ ] `cancel_deadline_min` が **1080**（前日18:00）に戻っている
- [ ] `seat_types` の active が正しい（カウンター8 / テーブル・個室34 / 2F宴会60）
- [ ] テスト用の予約・顧客データを削除した（一覧タブ→キャンセル→削除、顧客タブ→削除）
- [ ] Supabaseの新規サインアップが無効になっている（Authentication設定）
- [ ] DMARCレコードを設定済み（Cloudflare DNS: `_dmarc` TXT `v=DMARC1; p=none;`）
- [ ] 関数を最新でデプロイ済み（CORS強化を含むため全関数を再デプロイ）:
      `create-reservation` `get-availability` `cancel-reservation` `send-reminders` `admin-notify`（JWTあり）`line-webhook` `calendar-feed`

## B. 通しテスト（本番ドメインで）

- [ ] 2名予約 → 確認メール＋店側メール＋LINE通知＋購読カレンダー表示
- [ ] 9名予約 → 仮受付メール＋LINE【要電話確認】→ 管理画面で確定 → 確定メール
- [ ] キャンセルURL → キャンセル完了＋LINE通知＋カレンダーから消える
- [ ] 月曜がカレンダーで×、休業日設定が予約フォームに反映される
- [ ] 当日の予約ができない（前日締切）
- [ ] シークレットウィンドウで admin.html にデータが見えない
- [ ] iPhoneとAndroidで reserve.html の表示・操作に問題がない
- [ ] 前日リマインドが翌日分の予約に届く（cronの実行をSupabaseのLogsで確認）

## C. ホットペッパー併用の調整（公開直後にやる）

- [ ] HP管理画面のネット予約在庫を実際の空きより**少なめ**に設定
- [ ] スタッフ全員に運用マニュアルを共有（特に「HP・電話予約は必ず登録タブへ」）
- [ ] 店内・InstagramでWEB予約開始を告知（プロフィールのリンク更新）

## D. 公開後1〜2週間の様子見

- [ ] 毎日: LINE通知と店側メールが正常に届いているか
- [ ] 週1: `notification_log` で failed がないか確認（SQL: `select * from notification_log where status='failed' order by created_at desc;`）
- [ ] 週1: HP予約とシステムの突き合わせ
- [ ] メールの迷惑メール判定が続く場合はつるさんへ（DMARC強化を検討）
- [ ] 問題なく回り始めたら、HPのプラン見直し（管理費削減）を検討開始

## 将来のオプション（必要になったら）

- コース事前決済（Stripe）／キャンセル待ち／多言語対応
- LINE通知のグループ化（月200通超なら有料プラン検討）
- 公式LINEのお客様向け配信（同じチャネルで拡張可能）
